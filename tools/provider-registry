#!/usr/bin/env python3
"""Thin shim: provider-registry CLI -> provider_registry.cli::main.

Deployed to ~/.local/bin/provider-registry by the Makefile strip-loop
(no extension, so the strip is a no-op). Imports the library from the
user site-packages copy.
"""
import sys

from provider_registry.cli import main

sys.exit(main(sys.argv[1:]))
