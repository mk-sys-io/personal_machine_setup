# Enable IP forwarding for internet-netns veth traffic
# Packets from the namespace (10.0.4.2) arrive at veth-inet-host (10.0.4.1)
# on the host and must be forwarded to wlan0 (or other physical interface).
# Without this, all namespace traffic is silently dropped by the kernel.
net.ipv4.ip_forward = 1
