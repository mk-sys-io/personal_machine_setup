# Disable USB autosuspend for the btusb driver.
# Intel AX201 (and many other USB BT adapters) disconnect devices within seconds
# of connecting when autosuspend is enabled. The kernel suspends the USB device
# immediately, killing the Bluetooth connection.
options btusb enable_autosuspend=0
